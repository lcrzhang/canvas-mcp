# Canvas personal access token.
# canvas.uva.nl -> Account -> Settings -> New access token.
# Tokens expire after at most 90 days.
CANVAS_TOKEN=

# Optional: another Canvas instance. Defaults to https://canvas.uva.nl
# CANVAS_BASE_URL=https://canvas.uva.nl
